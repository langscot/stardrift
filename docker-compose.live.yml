version: '3.9'

services:
  bot-live:
    image: ghcr.io/langscot/stardrift:latest
    container_name: stardrift-bot-live
    restart: unless-stopped
    env_file:
      - .env.live
    environment:
      DATABASE_URL: postgresql://stellar:stellar_password@stardrift-postgres:5432/stardrift_live
      REDIS_URL: redis://stardrift-redis:6379/0
      NODE_ENV: production
    depends_on:
      - postgres
      - redis
    networks:
      - stardrift-network

networks:
  stardrift-network:
    external: true
    name: stardrift-network
